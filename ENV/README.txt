PUT YOUR REAL VALUES IN THE .env FILE IN THIS FOLDER.

For a normal Node host, move/copy ENV/.env to the project root as:
  horizon-character-bot/.env

For Cloudflare Workers, copy the SAME variable names/values into:
  Worker > Settings > Variables and Secrets

Keep DISCORD_TOKEN and SUPABASE_SERVICE_ROLE_KEY private.
